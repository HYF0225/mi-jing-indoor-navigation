# 建筑接入手册

## 已接入

1. OpenVGAL 艺术展厅：单层，MIT，保留原展示。
2. Empty Mall 三层商场：loafbrr，CC BY 4.0；1F / 2F / 3F。功能分区与商店 A–G 为测试标注。

试用顺序：选择 Empty Mall → 保持默认「1F 入口门厅 → 3F 影院 E」→ 比较东西扶梯路线 → 分层查看 → 播放 → 暂停 → 继续 → 导出视频 → 逐个节点确认。预览不会改变已确认位置。

## 新增一栋建筑

1. 取得可公开再分发的建筑模型与许可。保留原文件、固定来源、作者、许可、哈希；不得把仅有外壳的模型当作室内导航模型。
2. 保存 GLB 到 `public/models/`，并加入署名说明。模型路径经 `publicAsset` 处理，兼容 GitHub Pages 仓库子路径。
3. 在统一 `Building` 类型下新增数据文件：唯一建筑 ID、默认入口/目的地、楼层 ID/高度、模型范围、三维路径节点、边、目的地、原始扶梯/楼梯/电梯连接、几何依据、来源。
4. 模型坐标与导航坐标必须同系。核验楼板顶面、墙体、门洞、中庭空洞及垂直连接；不可直接把相同 XY/Z 的不同楼层点连起来。扶梯使用实际折线路径，不穿楼板；内部插值点设置 `checkpoint:false`。
5. 仅在用户能辨认的位置设置确认点。给出位置与朝向画面。店铺名称和房号应来自获准使用的真实清单；测试标签必须明确说明。
6. 在 `app/buildings.ts` 的 `BUILDINGS` 加入建筑配置。菜单、搜索、路线、确认、视频、日志和楼层选择共用现成组件，无需复制页面。
7. 运行 `validateBuilding`，补充模型几何校验及跨层、封路、无路线测试；运行 `pnpm test`、`pnpm exec tsc --noEmit`、两种生产构建。上线前需获得现场走测证据，不得将几何连通等同于真实可通行。
8. 发布到原 GitHub Pages 与私有 Sites，验证模型文件可加载。不要覆盖原建筑包。

## 本次限制

商场没有经过核验的电梯导航连接；因此避开扶梯与楼梯时，跨楼层返回无可用路线，不能虚构无障碍方案。扶梯几何当前演示双向连接，实际运营的上行/下行需现场核实后配置 `Connector.oneWay`。仅几何数据可重复验证，本次未新增浏览器交互自动化测试或实地走测。

本版是开发者通过建筑数据包接入，并非面向用户的自动上传/建图后台。后续真实项目仍需要资产采集、语义标注、版本审批和关闭通道管理。
